import React from "react";
import styles from "@/styles/PasswordModal.module.scss";

export default function PasswordModalComponent () {
    return (
        <div className={styles.backdrop}>

        </div>
    )
}